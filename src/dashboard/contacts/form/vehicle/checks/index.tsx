import { ReactElement } from "react";
import { observer } from "mobx-react-lite";

export const VehicleChecks = observer((): ReactElement => {
    return <div className='grid flex-column'></div>;
});
