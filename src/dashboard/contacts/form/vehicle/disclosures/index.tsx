import { ReactElement } from "react";
import { observer } from "mobx-react-lite";

export const VehicleDisclosures = observer((): ReactElement => {
    return <div className='grid  row-gap-2'></div>;
});
