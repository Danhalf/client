import { observer } from "mobx-react-lite";
import { ReactElement } from "react";

export const ExportWebDates = observer((): ReactElement => {
    return <div className='grid export-web-price row-gap-2'>dates</div>;
});
