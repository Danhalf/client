import { observer } from "mobx-react-lite";
import { ReactElement } from "react";

export const ExportWebLinks = observer((): ReactElement => {
    return <div className='grid export-web-price row-gap-2'>links</div>;
});
