import { observer } from "mobx-react-lite";
import { ReactElement } from "react";

export const ExportWebHistory = observer((): ReactElement => {
    return <div className='grid export-web-price row-gap-2'>history</div>;
});
