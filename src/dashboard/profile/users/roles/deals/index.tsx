import { observer } from "mobx-react-lite";
import { ReactElement } from "react";

export const RolesDeals = observer((): ReactElement => {
    return (
        <div>
            <div>Deals roles settings</div>
        </div>
    );
});
