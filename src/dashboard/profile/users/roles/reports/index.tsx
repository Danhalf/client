import { observer } from "mobx-react-lite";
import { ReactElement } from "react";

export const RolesReports = observer((): ReactElement => {
    return (
        <div>
            <div>Reports roles settings</div>
        </div>
    );
});
