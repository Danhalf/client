import { observer } from "mobx-react-lite";
import { ReactElement } from "react";

export const RolesContacts = observer((): ReactElement => {
    return (
        <div>
            <div>Contacts roles settings</div>
        </div>
    );
});
