import { observer } from "mobx-react-lite";
import { ReactElement } from "react";

export const RolesSettings = observer((): ReactElement => {
    return (
        <div>
            <div>Settings roles settings</div>
        </div>
    );
});
