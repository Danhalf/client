import { observer } from "mobx-react-lite";
import { ReactElement } from "react";

export const RolesOther = observer((): ReactElement => {
    return (
        <div>
            <div>Other roles settings</div>
        </div>
    );
});
