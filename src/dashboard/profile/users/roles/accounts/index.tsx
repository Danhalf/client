import { observer } from "mobx-react-lite";
import { ReactElement } from "react";

export const RolesAccounts = observer((): ReactElement => {
    return (
        <div>
            <div>Accounts roles settings</div>
        </div>
    );
});
