import { observer } from "mobx-react-lite";
import { ReactElement } from "react";

export const RolesInventory = observer((): ReactElement => {
    return (
        <div>
            <div>Inventory roles settings</div>
        </div>
    );
});
