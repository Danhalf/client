import { ReactElement } from "react";
import { observer } from "mobx-react-lite";

export const BackendOptions = observer((): ReactElement => {
    return <div className='backend-options'></div>;
});
