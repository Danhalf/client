import { ReactElement } from "react";
import { observer } from "mobx-react-lite";

export const TypicalOptions = observer((): ReactElement => {
    return <div className='typical-options'></div>;
});
