import { ReactElement } from "react";
import { observer } from "mobx-react-lite";

export const AdditionalInformation = observer((): ReactElement => {
    return <div className='additional-information'></div>;
});
